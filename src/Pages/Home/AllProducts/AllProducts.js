import React from 'react';

const AllProducts = (booksData) => {
    console.log(booksData);
    return (
        <div>
            <h2>All Books</h2>

        </div>
    );
};

export default AllProducts;